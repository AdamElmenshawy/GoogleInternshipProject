/**
 * AOSP Build Blueprint for the WebP Image Parser Fuzzer.
 *
 * Build: lunch aosp_cf_x86_64-userdebug && m webp_parser_fuzzer
 */
cc_fuzz {
    name: "webp_parser_fuzzer",
    host_supported: false,
    device_supported: true,

    srcs: [
        "webp_parser_fuzzer.cpp",
    ],

    static_libs: [
        "libwebp-decode",
    ],

    shared_libs: [
        "liblog",
        "libutils",
    ],

    dictionary: "webp.dict",

    fuzz_config: {
        cc: [
            "android-security@google.com",
        ],
        componentid: 25690,
        description: "Fuzzer targeting WebP RIFF VP8/VP8L lossless chunk parsing",
        vector: "remote",
        service_privilege: "unprivileged",
    },

    sanitize: {
        hwaddress: true,
        address: true,
    },
}
